{% block form_row %}
    <div class="label">
        {{ form_label(form) }}          <!-- variable par défaut qui sera remplacée -->
    </div>
    <div>
        <div class="error">{{ form_errors(form) }} </div>
        <div class="widget">{{ form_widget(form) }} </div>
    </div>
{% endblock %}